'use strict';

// Configuring the Crawls module
angular.module('crawls').run(['Menus',
  function (Menus) {
    /*// Add the crawls dropdown item
    Menus.addMenuItem('topbar', {
      title: 'Crawls',
      state: 'crawls',
      type: 'dropdown',
      roles: ['*']
    });

    // Add the dropdown list item
    Menus.addSubMenuItem('topbar', 'crawls', {
      title: 'List Crawls',
      state: 'crawls.list'
    });

    // Add the dropdown create item
    Menus.addSubMenuItem('topbar', 'crawls', {
      title: 'Create Crawls',
      state: 'crawls.create',
      roles: ['user']
    });*/
  }
]);
